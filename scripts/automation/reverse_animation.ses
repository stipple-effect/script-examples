() {
    ~ project p = $get_project();
    ~ int fc = $get_frame_count(p);

    for (int ops = fc - 1; ops > 0; ops--) {
        $set_frame_index(p, 0);

        for (int op = 0; op < ops; op++)
            $move_frame_forward(p);
    }
}
